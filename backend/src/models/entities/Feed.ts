export interface Feed {
    userId: string
    feedId: string
    createdAt: string
    updatedAt: string
    caption: string
    attachmentUrl?: string
    comments: number
    reaction: number
  }
  